<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatisorg/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	

	<select id="listAll" parameterType="BoardVO" resultType="BoardVO">
		SELECT * FROM BOARD 
		WHERE BOARD_USE = 'Y'
		AND CATEGORY=#{category}
		ORDER BY BOARD_NUMBER DESC
		LIMIT #{perpagenum} OFFSET #{startpage}
	</select>
	
	<insert id="insert" parameterType="BoardVO" >
		INSERT INTO BOARD (
			CATEGORY,
			TITLE,
			CONTENT,
			WRITER,
			WRITE_DATE,
			SEARCH_KEY,
			BOARD_USE,
			VIEW_CNT
		) VALUES(
			#{category},
			#{title},
			#{content},
			#{writer},
			now(),
			#{search_key},
			'Y',
			0
		)
	</insert>
	
	<select id="select_vo" parameterType="BoardVO" resultType="BoardVO">
		SELECT * FROM BOARD WHERE BOARD_NUMBER = #{board_number}
	</select>
	
	<update id="update_viewcnt" parameterType="BoardVO" >
		UPDATE BOARD SET VIEW_CNT = VIEW_CNT+1
		WHERE
		BOARD_NUMBER = #{board_number}
	</update>
	
	<update id="update" parameterType="BoardVO">
		UPDATE BOARD SET
		TITLE = #{title}, CONTENT = #{content}
		WHERE BOARD_NUMBER = #{board_number}
	</update>
	
	<delete id="delete" parameterType="BoardVO">
		DELETE
		FROM BOARD
		WHERE BOARD_NUMBER = #{board_number} 
	</delete>
	
	<select id="list_count" resultType="INT">
		SELECT COUNT(*) FROM BOARD
		WHERE BOARD_USE='Y'
		AND CATEGORY=#{category}
	</select>
	
	
	<!-- 자유게시판 -->
	<select id="free_listAll" parameterType="BoardVO" resultType="BoardVO">
		SELECT * FROM BOARD 
		WHERE BOARD_USE = 'Y'
		AND CODE='TM002'
		ORDER BY BOARD_NUMBER DESC
		LIMIT #{perpagenum} OFFSET #{startpage}
	</select>
	
	
	<select id="free_list_count" resultType="INT">
		SELECT COUNT(*) FROM BOARD
		WHERE BOARD_USE='Y'
		AND CODE='TM002'
	</select>
	
	<insert id="free_insert" parameterType="BoardVO" >
		INSERT INTO BOARD (
			CODE,
			TITLE,
			CONTENT,
			WRITER,
			WRITE_DATE,
			SEARCH_KEY,
			BOARD_USE,
			VIEW_CNT
		) VALUES(
			'TM002',
			#{title},
			#{content},
			#{writer},
			now(),
			#{search_key},
			'Y',
			0
		)
	</insert>
</mapper>